import { combineReducers } from 'redux'
import kingdomAlignment from '../components/KingdomAlignment/reducer'

const rootReducer = combineReducers({
  kingdomAlignment
})

export default rootReducer;
